v6

## Changes

* Adds support for two new Nix versions: `2.3.12` and `2.4pre20210601_5985b8b`.
  All previously supported Nix versions are still supported.

* Bumps the default Nix version from `2.3.7` to `2.3.12`.
